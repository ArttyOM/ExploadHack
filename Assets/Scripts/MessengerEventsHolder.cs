using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MessengerEventsHolder{

    public enum UIButtons { NotSelected, InventoryBTN, RunAwayBTN}

    public enum QuestInfoViewButtons { NotSelected, AcceptFight, DeclineFight, ShowQuestInfoView }

    public enum GameViewVisual { InstaRunAwayCost, HPChanged, XPChanged, StartDodgeCD, EndDodgeCD }
        
}
